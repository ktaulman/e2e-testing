version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          name: starting server
          pre-steps: 
            - checkout 
            - run: cd back-end 
            - start: npm run dev 
            - wait-on: 'http://localhost:4000'

          

    
